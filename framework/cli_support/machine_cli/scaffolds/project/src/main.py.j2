"""Main entry point for {{ project_name }}."""
from machine_core import Machine

machine = Machine(name="{{ project_name }}")

# Register your agents, tools, and workflows here:
# from .agents.example import example_agent
# machine.register("agent", "example", example_agent)
