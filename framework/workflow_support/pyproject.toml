[project]
name = "machine-core-workflow-support"
version = "0.9.0"
description = "Defines the workflow and execution-engine categories for machine-core"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
