[project]
name = "machine-core-storage-support"
version = "0.1.0"
description = "Storage backend implementations: local and S3"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
