[project]
name = "machine-core-channel-support"
version = "0.1.0"
description = "Channel category: in-memory and WebSocket channels"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
