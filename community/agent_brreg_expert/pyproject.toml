[project]
name = "agent_brreg_expert"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27", "tqdm>=4.60"]


[tool.hatch.build.targets.wheel]
packages = ["."]
sources = {"." = "agent_brreg_expert"}


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
