[project]
name = "machine-core-deployer-support"
version = "0.1.0"
description = "Deployment target category and built-in deployers for machine-core"
requires-python = ">=3.12"
dependencies = ["httpx>=0.27.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
