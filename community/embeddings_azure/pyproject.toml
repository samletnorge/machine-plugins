[project]
name = "machine-core-embeddings-azure"
version = "0.1.0"
description = "Azure OpenAI embedding provider"
requires-python = ">=3.12"
dependencies = ["httpx>=0.28"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
