[project]
name = "agent_runtime_basic"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []


[tool.hatch.build.targets.wheel]
packages = ["agent_runtime_basic"]


[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel.force-include]
"manifest.json" = "agent_runtime_basic/manifest.json"
