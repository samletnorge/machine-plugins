[project]
name = "machine-core-provider-groq"
version = "0.1.0"
description = "Groq LLM provider for machine-core"
requires-python = ">=3.12"
dependencies = ["pydantic-ai>=0.0.1"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
